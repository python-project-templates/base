export default function App() {
  return (
    <main>
      <h1>{{ brand_name }}</h1>
      <p>{{ project_description }}</p>
      <a className="button" href="#develop">
        Get started
      </a>
    </main>
  );
}
