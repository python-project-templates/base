<svelte:head>
  <title>{{ brand_name }}</title>
  <link rel="canonical" href="{{ site_url }}/" />
</svelte:head>

<main class="mx-auto max-w-5xl px-6 py-24">
  <p class="text-sm uppercase tracking-widest">{{ brand_name }}</p>
  <h1 class="mt-6 text-6xl font-semibold tracking-tight">{{ project_name }}</h1>
  <p class="mt-6 max-w-2xl text-xl leading-relaxed">{{ project_description }}</p>
</main>
